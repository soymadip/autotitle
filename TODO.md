- [x] Add a volatile cache for search results that will be cleared upon prompt exit.
- [x] when selection series comes, there is no indication that those can be filtered when starts typing

- [x] The search(`WithProvider(str)`) make it list/array instead of string. so we can query multiple providers.
- [x] Thse searchStream should have this too
